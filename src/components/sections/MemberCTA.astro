---
import Container from '@/components/ui/Container.astro';

interface Props {
  title: string;
  description?: string;
  buttonLabel: string;
  buttonHref: string;
}

const { title, description, buttonLabel, buttonHref } = Astro.props;
---

<section class="member-cta" aria-labelledby="member-cta-title">
  <Container>
    <div class="member-cta__grid">
      <div>
        <h2 id="member-cta-title" class="member-cta__title">{title}</h2>
        {description && <p class="member-cta__desc">{description}</p>}
      </div>
      <a href={buttonHref} class="member-cta__btn">
        {buttonLabel}
        <span aria-hidden="true">→</span>
      </a>
    </div>
  </Container>
</section>

<style>
  .member-cta {
    background: var(--color-bg);
    border-top: 3px solid var(--color-primary);
    padding: var(--space-8) 0;
  }

  .member-cta__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-5);
    align-items: center;
  }

  .member-cta__title {
    margin: 0 0 var(--space-2);
    font-size: clamp(1.5rem, 3vw, 2rem);
  }

  .member-cta__desc {
    color: var(--color-text-muted);
    font-size: var(--text-base);
    line-height: var(--leading-normal);
    margin: 0;
    max-width: 50ch;
  }

  .member-cta__btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-3);
    min-height: 52px;
    padding: 0 var(--space-6);
    background: var(--color-primary);
    color: var(--color-text-invert);
    font-weight: var(--weight-bold);
    font-size: var(--text-sm);
    letter-spacing: 0.05em;
    text-transform: uppercase;
    text-decoration: none;
    border-radius: var(--radius-md);
    justify-self: start;
    transition: background var(--transition-fast);
  }

  .member-cta__btn:hover,
  .member-cta__btn:focus-visible {
    background: var(--color-primary-hover);
  }

  @media (min-width: 700px) {
    .member-cta {
      padding: var(--space-10) 0;
    }

    .member-cta__grid {
      grid-template-columns: 1fr auto;
      gap: var(--space-8);
    }

    .member-cta__btn {
      justify-self: end;
    }
  }
</style>
